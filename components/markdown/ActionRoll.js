export const ActionRoll = ({ children }) => (
  <span
    style={{
      color: "cornflowerblue",
      fontFamily: '"Cinzel", serif',
      fontWeight: "bold",
    }}
  >
    {children.slice(1)}
  </span>
);
